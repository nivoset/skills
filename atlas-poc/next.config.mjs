/** @type {import('next').NextConfig} */
const nextConfig = {
  // The POC reads the /atlas content directory straight off disk at request
  // time (see lib/atlas/read.ts) — nothing here needs to change for that.
};

export default nextConfig;
