const crypto=require('crypto'),fs=require('fs'); const canonicalize=x=>Array.isArray(x)?'['+x.map(canonicalize)+']':x&&typeof x==='object'?'{'+Object.keys(x).sort().map(k=>JSON.stringify(k)+':'+canonicalize(x[k])).join(',')+'}':JSON.stringify(x); exports.freeze=(r,d)=>{let c=canonicalize(r),h=crypto.createHash('sha256').update(c).digest('hex');fs.writeFileSync(d+'/recipe.frozen.json',c);fs.writeFileSync(d+'/recipe.sha256',h);return h};
